---
doc: SHIOJI_PROCESS
layer: L1(本キットの仕様 — 編集は人間のみ)
version: 0.1
status: active
date: 2026-07-12
---

# PROCESS.md — 潮路の仕様(なぜこの形か)

本書は潮路(shioji)が生成するプロセスの**意図と根拠**を定める。生成先プロジェクトの規範は各プロジェクトの CLAUDE.md(L0)であり、本書はその設計図である。出自は tsukishio の運用実績(参照実装)。

## 1. 中心思想 — 規則は強制装置とペアでのみ存在する

エージェント駆動開発が壊れる典型は「文書はあるが守られない」である。潮路の規則は原則として**機械的強制の対応物**(pre-commitフック・CIゲート・検査スクリプト)とペアで出荷される。強制装置を割り当てられない規則を導入するときは、「これは紙の契約である」と明示する(インセプション台本がこれを強制的に問う)。

第二の思想は**リポジトリが唯一の記憶**であること。エージェントのセッションは揮発する。進捗(PROGRESS)・疑義(ISSUES)・判断(DECISIONS)・状態(TASK_INDEX の status)を残さない作業は、存在しなかった作業として扱われる。

## 2. 文書レイヤ(全プロジェクト共通)

| レイヤ | 文書 | 役割 | エージェント編集 |
|---|---|---|---|
| L0 | CLAUDE.md / AGENTS.md | 手続きの最上位契約 | 禁止 |
| L1 | docs/spec/* | 規範(何を作るか) | 禁止(提案は docs/proposals/) |
| L2 | docs/design/* | 記述(詳細・トークン) | PRで提案可(根拠必須) |
| L3 | TASK_INDEX.md の status / docs/L3/* | 運用・動的 | 更新義務 |

編集権限の非対称性が本質である: **上位ほど人間専管、下位ほどエージェント義務**。矛盾の発見時は黙って解決せず ISSUES に記録する。

## 3. 二段階インスタンス化

- **Stage 1(機械)**: copier による決定論的コピー。パラメータは機械的なもののみ(名前・REQ接頭辞・スタック)。生成直後に `make ci` が緑であること — **0日目から緑の原則**。スタック未初期化の間、スタック系検証は ready_marker 不在により自動skipされる(tsukishio の「flutter未初期化skip」の一般化)。
- **Stage 2(意味)**: INCEPTION_PROMPT.md による仕様注入。エージェントの出力は**すべて docs/proposals/inception/ への提案**であり、docs/spec/ と CLAUDE.md への反映は人間が行う。この転記行為そのものが承認ゲートである。骨格をLLMに再生成させない理由: 版ごとのドリフトを防ぎ、骨格の改良を copier update で全プロジェクトに配布可能に保つため。

## 4. project.yaml — プロセス設定のSSoT

プロジェクト固有値(REQ接頭辞・仕様書glob・src/test層・ゲート列・スタックコマンド・オラクル定義)は project.yaml に一元化し、汎用スクリプトはこのファイルだけを読む。これにより copier update がプロジェクト固有値を上書きしない(project.yaml は生成後プロジェクト側の所有物)。

`oracle:` の許容誤差・合格率は、一度人間が確定したら**変更禁止**(§6-N1)。gates への行追加はエージェント提案可(DECISIONS 記録)。

## 5. 検証ゲートの規約

1. **三モード規約**: 精度系ゲートは `--dry-run`(参照未投入でも緑・レポートのみ)→ `--gate`(本判定)→ `--selftest`(判定ロジック自体の検査)を備える。**dry-run→gate の切替は人間のみ**が行う。
2. **参照オラクルは人間専管**: verification/reference/ 配下の生成・改変はエージェント禁止。オラクルをエージェントが書けるなら、それはオラクルではない。
3. **不合格の検出は成果**: ゲート不合格時に閾値・参照データ・テストを調整して合格させることを禁ずる。原因分析を ISSUES に記録して停止(自走時は当該系統のみ停止)。
4. **同一ゲート列を三箇所で実行**: make guard / pre-commit フック / CI guardrails は project.yaml の同じ gates 列を実行する(scripts/run_gates.py)。ローカルとCIの乖離を作らない。
5. **未装備と合格を分ける**: 「実行していない」ことと「実行して合格した」ことを、同じ緑にしてはならない。ゲートは終了コード `3` で未装備を自己申告し、`run_gates.py` は `緑N件 / 未装備M件` として報告して `verification/gate_status.json` に残す。未装備には project.yaml の `by_task` で期限を与え、期限のタスクが `done` になったのに装備されていなければ赤にする(カットオーバー忘れの検出)。`--strict` は未装備自体を赤とする(リリース前・監査時に人間が実行する。0日目の main を赤にしないため、生成CIの既定には入れない)。0日目から緑の原則は維持するが、**その緑が何を意味するかを常に区別できる状態に保つ**。
   - 背景: GitHub Actions は `if:` でスキップされたジョブを Success として報告し、required status check に指定していてもマージを妨げない(再現: [hroabe/verifier-integrity-demo](https://github.com/hroabe/verifier-integrity-demo))。**同じ誤りをゲート実行器の側で繰り返さない。**
6. **設定はSSoTであり、検査される**: `project.yaml` 自体を `scripts/check_project_config.py` が検査する。これは `gates` 列に**書かない** — `gates` 自体が `project.yaml` の中にあるため、そこに置くと「設定を検査する仕組み」を設定から消せてしまう。`run_gates.py` が `gates` 列より前に無条件で実行する。未知の `match`、範囲外の `pass_rate`、v1 の残骸などを、重い検査の前に落とす(fail fast)。設定を読むだけで実行してはならない。
7. **ゲートは argv で実行し、shell を経由しない**: `project.yaml` はエージェントが編集できる。そこに書かれた文字列をシェルに解釈させない。複雑な処理はシェルの一行ではなく `scripts/*.py` へ置く。
8. **有効な仕様書は1つを明示する**: glob で自動選択しない。文字列ソートでは `v0.10` が `v0.9` より前に並び、front-matter の `status`(superseded / draft)も見られない。`requirements.active_spec` に人間が明示し、実在と `status: active` を検査する。未設定の間、REQ整合ゲートは**未装備**として報告する(緑には数えない)。

9. **照合するのは常にいまのコードの出力**: オラクルの予測は毎回作り直す(`oracle.generate` を必須とする)。再生成しないと、正しい実装で予測を作って合格させたあと実装を壊しても、古い予測が残って合格し続ける。生成の失敗は「未装備」ではなく赤(ハーネスが壊れている)。
10. **空の対象で合格させない**: 参照イベントが0件の系列は「全問正解」ではなく「判定できない」。合格にしない。従来は合格率を 1.0 として扱っていた。
11. **取りこぼしと誤検出を分けて測る**: recall(TP/参照件数)だけで判定すると、誤検出を何件出しても合格する。precision(TP/予測件数)を判定に含められるようにし、含めていない設定は検査が警告する。許容外の対応ペアは FN かつ FP として数える。


## 6. 規範カタログ — 絶対規範の一般形

生成先 CLAUDE.md §2 は、以下のカタログから採否・具体化して構成する(インセプション台本が1項ずつ問う)。N1とN4は全プロジェクト共通で事前配線される。

| # | 一般形 | 問い | 強制装置の例 |
|---|---|---|---|
| N1 | 真実性 | 曲げてはいけない数字・事実は何か。参照オラクルと許容誤差は | validate_oracle --gate |
| N2 | 出所台帳 | 外部データ・素材は何を使い、出典文言を誰が確定するか | 台帳照合・原本digest検査 |
| N3 | 法務ゲート | 名称・商標・規制・審査の確定前に禁止される露出は | リリースゲートの文字列検査 |
| N4 | 廃止ID衛生 | 廃止した仕様への参照を検出するか(常にyes) | check_req_links(標準装備) |
| N5 | 能力制約 | オフライン・PII不収集・テレメトリ禁止等の「原則」は | 依存・権限・I/Oの静的検査テスト |
| N6 | 商業倫理 | 課金があるなら、指示されても実装を拒否する操作は | レビュー規範+文言検査 |
| N7 | 二重化義務 | 表現を追加するたび同一PRで義務付ける等価物は(a11y等) | 等価物の存在テスト |
| N8 | 免責固定 | 削除・弱体化を禁じる安全文言は | 文言存在テスト |

## 7. 自走モードの意図(生成先 §3.5)

tsukishio で実証した12規則を無改変で継承する。設計意図の要点:

- **止まらないこと**より**握り潰さないこと**を優先する: 疑義は ISSUE+blocked で迂回し、全体は進める。
- **可逆な判断は記録して進む**(DECISIONS 1行+戻し方)。不可逆・外部波及は人間へ。
- **沼防止**: 同種の失敗3回で blocked。無限リトライはトークンと信頼の浪費。
- **昇格の非対称**: 担当=A のみ自律 done 可。A+H は review 止まり、H には触れない。
- **セッション終端の義務**: 未コミットゼロ+PROGRESS 冒頭「次の一手」1行。記憶はリポジトリに置く。

## 8. 人間ゲート(全プロジェクト共通の最小集合)

- L0/L1 の編集(インセプション出力の spec / CLAUDE.md への反映を含む)
- 精度ゲートの --dry-run→--gate 切替
- 参照オラクル(verification/reference/)の投入
- アプリ本体の依存台帳(project.yaml の stack.app_manifest が指すファイル)への追加
- ゴールデン既存基準の変更(採用時。初回生成は自律可・CHECKPOINT列挙)
- リリースタグ・ストア/本番提出

プロジェクト固有の人間ゲートはインセプションで追加する。

## 9. 版管理と還流

- 本キットは SemVer でタグを打つ(タグ付けは人間)。導出プロジェクトは .copier-answers.yml に版が記録され、`copier update` で追随する。
- **還流の義務感**: 導出プロジェクトの運用でプロセス上の改善(ゲートの強化・書式の改良)を見つけたら、そのプロジェクトに閉じず本キットへPRする。tsukishio の実例: 精度ゲートの整列方式強化、ゴールデン基準のプラットフォーム分離 — いずれも運用が設計を改良した。
- 仕様内容(プロジェクト固有)はテンプレートに混入させない。混入すると update が全プロジェクトを汚染する。

## 10. 参照実装

tsukishio(https://github.com/hroabe/tsukishio)。本キットの全規則は同リポジトリの CLAUDE.md v1.5 / INITIAL_PROMPT v2.4 / scripts 群を原型とし、汎用化にあたっての抽出判断は同リポジトリ docs/proposals/2026-07-12_process_kit_extraction.md に記録されている。
