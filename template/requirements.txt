# ツールチェーン依存のSSoT(CLAUDE.md §8)。エージェントは追加時に理由コメントを添えること。
# venv外インストール禁止。アプリ本体の依存(stack.app_manifest)はここではなく人間ゲート(§6)。
pyyaml==6.0.2  # project.yaml(プロセス設定のSSoT)の読取。更新は DECISIONS に1行(再現性 — 版の固定はキット自身にも課す)
